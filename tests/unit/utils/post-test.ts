import post from 'ember-blog/utils/post';
import { module, test } from 'qunit';

module('Unit | Utility | post', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = post();
    assert.ok(result);
  });
});
