<article local-class="post">
	<Button @text="Edit" @icon="pencil-alt" />
	<h1>
		{{@post.title}}
	</h1>
	<em>
		{{@post.createdAt}}
	</em>
	<div>
		{{html-safe @post.content}}
	</div>
</article>
{{yield}}